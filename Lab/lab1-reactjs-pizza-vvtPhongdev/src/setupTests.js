// Create React App tự động load file này trước mỗi lần chạy test.
// Thêm custom matchers của @testing-library/jest-dom vào Jest
// (ví dụ: toBeInTheDocument, toHaveTextContent, ...)
import '@testing-library/jest-dom'
